# dependencies
node_modules
.pnp
.pnp.js

# build
.next
out
dist
build

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env — no subir secretos (*.example sí va al repo)
.env
.env*.local
backend/.env
infra/.env

# IDE
.idea
.vscode
*.swp

# OS
.DS_Store
Thumbs.db

# Documentación académica local (no subir propuestas sensibles al repo público)
docs/PROPUESTA_ACADEMICA*.md

# Datos de laboratorio (.mat) — no subir al repo
data/**/*.mat
*.mat

# Registro de avances (`docs/REGISTRO_AVANCES.md`): sí se versiona para equipo + seguimiento.
